# 匿名数据集说明

| 样本 | 形态 | 准入与校验 | Analysis Units |
| --- | --- | --- | --- |
| sample-A | 真实结构化业务表格 | 已完成 Managed Source 准入、不可变副本与内容校验；不公开原始标识或内容 | 105 个 eligible，34 个 excluded；4 个工作表 |

筛选过程为只读探查。扫描型 PDF 候选因无可抽取文本被排除；未把任何候选自动准入。sample-A 是唯一获得明确授权并进入本实验的一份样本。

样本正文、名称、路径、哈希、单元格值、业务实体与模型输出均不进入仓库。
